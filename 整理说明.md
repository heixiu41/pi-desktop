# PiDesktop-项目（必要文件整理版）

从 `C:\Users\Administrator\Desktop\UI` 提取的必要源码文件（2025-08-18 整理）。
已排除：`node_modules/`（依赖包，需重新安装）、测试脚本（node-test.mjs / demo-kb.mjs / send-verify.mjs）、
`test-anim/`、`locate-anything.cpp-master/`（外部 C++ 项目）、备份文件（*.bak-loop）等非必要内容。

## 目录结构

```
PiDesktop-项目/
├── main.js                  # Electron 主进程入口
├── preload.js               # 预加载脚本（IPC 桥接）
├── auto-config.js           # 🔧 自动检测 + 自动配置模块（新电脑免配置）
├── bootstrap/agent/         # 自动配置模板（auth/models/settings）
├── package.json             # 依赖声明
├── package-lock.json        # 依赖锁定
├── README.md                # 项目说明
├── 启动Pi桌面应用.bat        # 启动脚本
├── 重启Pi桌面应用.bat        # 重启脚本（先关旧再启新）
├── download-manager.js      # 受管下载
├── download-tool.js         # 下载工具（Pi 工具）
├── format.js                # 格式化工具
├── rag-engine.js            # 知识库 RAG 引擎
├── embed-worker.mjs         # 嵌入子进程（RAG 向量化）
├── summary.js               # 今日总结
├── nodes/                   # 节点系统引擎
│   ├── node-registry.js     # 节点注册表
│   ├── blueprint-manager.js # 蓝图管理（执行引擎）
│   ├── ai-tools.js          # 节点 AI 工具
│   ├── kb-tools.js          # 知识库工具
│   ├── system-prompt.js     # 系统提示词
│   └── vision-tool.js       # 视觉工具
├── nodes-docs/              # AI 说明书（带分类/重要级标记）
└── renderer/                # 界面（聊天 + 节点画布）
    ├── index.html / app.js / style.css
    ├── canvas.html / canvas.js / canvas.css
    └── icons.html(未复制，见下) / icons.js / icons.css
```

## ✨ 启动动画（v1.6：简化版——平滑放大）
加载窗口（`renderer/splash.html` + `splash.css` + `splash.js`）→ 初始化完成后：
1. `splash:prepare` 预置双轴初始缩放（sx=420/1180, sy=300/820，媒体查询防闪帧）
2. 加载窗口一次性 `setBounds` 到主窗口尺寸（无逐帧窗口操作）
3. `splash:zoom`：内容 rAF + GPU transform 从屏幕中心平滑放大（560ms easeOutCubic）
4. 放大完成 → 主窗口就位显示 → `splash:fade` 淡出 → 关闭加载窗口
直角窗口、无图标移动、无圆角。实测：全流程正常，主窗口就绪。

> 🐛 顺带修复：`blueprint-manager.js` 的目录监听 `for await` 会阻塞 `init()` 导致 setup 卡在"加载蓝图"阶段（Windows 下无文件事件时永不返回）——已改为后台异步监听，启动不再卡住。

## ⚙️ API 配置界面（v1.3 新增）
侧边栏「API 配置」页：状态总览 / Key 管理（保存·测试连接·环境变量填入·显示切换）/ 默认模型（下拉+卡片点击应用）/ 环境信息。
新增 IPC：`api:get-state`、`api:save-key`、`api:test`、`api:set-default-model`、`api:env-key`（preload 已暴露）。
已实测：14 项 UI/功能测试全部通过（含真实连接测试 394ms）。

## 🔧 自动检测 + 自动配置功能（v1.2 更新：Key 已从项目移除）

启动时 `main.js` 自动调用 `auto-config.js` 的 `ensurePiConfig()`：
1. **检测** `~/.pi/agent/` 下 auth.json / models-store.json / settings.json 与运行时目录
2. **缺失自动补齐**：创建 `~/.pi/{nodes,nodes-docs,blueprints,kb,summary}` 目录 + 从 `bootstrap/agent/` 模板复制配置
3. **API Key 安全三路**（🔐 项目内不存 Key）：
   - 本机已有有效 Key → 不动
   - 环境变量 `DEEPSEEK_API_KEY` → 启动自动生成 auth.json
   - 都没有 → 写入空占位，聊天区提示手动配置
4. **不覆盖已有配置**：仅补缺失文件，幂等

已实测：全新空目录 + 环境变量 → 自动完成全部配置；无 Key → 明确提示。

> 🔐 注意：本文件夹不包含 API Key。`bootstrap/agent/auth.json` 是空占位。

## 如何运行

```bash
cd PiDesktop-项目
npm install        # 先安装依赖（会重新生成 node_modules）
npm start          # 或双击 启动Pi桌面应用.bat
```

## 与原项目的差异

| 项目 | 说明 |
|------|------|
| node_modules | 已排除，需 `npm install` 重新安装 |
| nodes/blueprint-manager.js.bak-loop | 备份文件，已排除 |
| renderer/icons.html | 未在 renderer 目录中（原项目 icons.html 位于 test-anim/，属测试用） |
| locate-anything.cpp-master | 外部 C++ 项目，与本应用无关，已排除 |
| 运行时数据 | 会话/蓝图/节点/知识库均存于 `~/.pi/`，不在项目目录内 |

> 注意：改主进程文件（main.js、nodes/*.js）后需重启应用；改渲染器（renderer/*）重载对应窗口即可。
