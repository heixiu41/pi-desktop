# 拖拽控制下载窗口 —— 实现方法论

> 适用范围：本项目（Electron + Pi SDK 桌面应用）的「下载面板停靠 / 悬浮窗口」体系
> 关联代码：`main.js`（主进程）、`preload.js`（IPC 桥）、`renderer/`（界面）

---

## 一、需求与目标

**需求**：用户可以通过拖拽控制下载窗口——悬浮窗口可拖动移动、可调整大小、可最小化、可停靠回主窗口。

**现状盘点**（改动前）：

| 能力 | 状态 | 实现方式 |
|---|---|---|
| 面板停靠（左右上下） | ✅ 已有 | `applyDockSide()` + CSS |
| 拖拽调整停靠面板大小 | ✅ 已有 | `dockHandle` pointer 事件 |
| 悬浮成独立窗口 | ✅ 已有 | `dl:float` IPC → `openDockWindow()` |
| 拖拽移动悬浮窗口 | ⚠️ 依赖系统标题栏 | 标准窗口默认行为 |
| 悬浮窗口最小化 | ❌ 无 | — |

**目标**：悬浮窗口改为无边框（frameless），面板头部整行可拖拽移动窗口，补齐最小化；并把整个过程沉淀为可复用的方法论。

---

## 二、架构基础：进程边界（先想清楚再动手）

Electron 是双进程模型，**窗口操作（移动/最小化/置顶）只能在主进程执行**：

```
┌─ 渲染进程（renderer/）─────────────┐      ┌─ 主进程（main.js）──────────┐
│ UI 交互、CSS 拖拽区域声明            │ IPC  │ 窗口管理、下载管理            │
│ preload 桥：window.pi.dlMinimize()  │ ───► │ ipcMain.on("dl:minimize")    │
└─────────────────────────────────────┘      └─────────────────────────────┘
```

三个文件各司其职，改动必须三处同步：

| 文件 | 职责 | 本需求改动 |
|---|---|---|
| `main.js` | 创建窗口、响应 IPC | `frame: false`；新增 `dl:minimize` |
| `preload.js` | 安全暴露 API（contextBridge） | 暴露 `dlMinimize()` |
| `renderer/` | 拖拽区域声明 + 按钮绑定 | CSS app-region；头部把手；最小化按钮 |

---

## 三、方案选型：拖拽移动的三种做法

| 方案 | 原理 | 优点 | 缺点 | 结论 |
|---|---|---|---|---|
| **A. 无边框 + `-webkit-app-region: drag`** | CSS 声明拖拽区域，OS 原生拖拽 | 零 JS、最顺滑、无延迟 | 失去系统标题栏；区域内按钮必须 `no-drag` | ✅ **采用** |
| B. 保留标题栏 + JS 模拟拖拽 | pointer 事件 → IPC → `win.setPosition` | 保留系统标题栏 | 复杂、有延迟、要处理屏幕边界/多显示器 | ✗ |
| C. 维持现状（系统标题栏拖） | 默认行为 | 零改动 | 拖拽区域小、体验不现代 | ✗ |

**选 A 的理由**：`app-region: drag` 是 Chromium 原生能力，拖拽交给操作系统，顺滑度是 JS 模拟无法比的。代价（frameless 失去标题栏）通过「头部整行可拖 + 补齐最小化/关闭按钮」弥补。

---

## 四、实现步骤（四步走）

### Step 1 主进程：无边框窗口 + 最小化 IPC

`main.js` —— 悬浮窗口去掉系统边框：

```js
dockWindow = new BrowserWindow({
  width: 480, height: 640,
  minWidth: 320, minHeight: 380,
  frame: false,                     // ← 无边框：头部 app-region: drag 负责移动
  backgroundColor: "#101418",
  autoHideMenuBar: true,
  webPreferences: { /* ...不变 */ },
});
```

新增窗口操作 IPC（**窗口操作必须走主进程**）：

```js
ipcMain.on("dl:minimize", () => {
  if (dockWindow && !dockWindow.isDestroyed()) dockWindow.minimize();
});
```

### Step 2 预加载：暴露桥方法

`preload.js`：

```js
dlMinimize: () => ipcRenderer.send("dl:minimize"),
```

### Step 3 渲染：拖拽把手 + 最小化按钮

`index.html` —— 面板头部加把手（仅悬浮模式显示），控制区加最小化：

```html
<div class="dl-header">
  <span class="drag-grip" id="dragGrip" title="拖动此处移动窗口">⠿</span>
  <h2>...</h2>
  ...
  <div class="float-controls" id="floatControls" hidden>
    <button id="dockMinBtn" class="btn small" title="最小化">─</button>
    <button id="dockDockBtn" ...>◧ 停靠回主窗</button>
    <button id="floatCloseBtn" ...>✕</button>
  </div>
</div>
```

`app.js` —— 悬浮分支显示控制按钮 + 绑定最小化：

```js
if (isFloat) {
  document.body.classList.add("float-mode");
  dockPanel.hidden = false;
  floatControls.hidden = false;   // ← 关键：见踩坑 #1
}
dockMinBtn.addEventListener("click", () => window.pi.dlMinimize());
```

### Step 4 样式：拖拽区域三件套

`style.css` —— **声明拖拽区域 / 按钮反声明 / 禁止选中**：

```css
/* 悬浮窗口无边框：头部整行作为拖拽手柄 */
.drag-grip { display: none; }                       /* 默认隐藏，仅悬浮显示 */
body.float-mode .drag-grip { display: inline-flex; }
body.float-mode .dl-header {
  -webkit-app-region: drag;   /* ① 声明拖拽区域 */
  cursor: grab;
  user-select: none;          /* ③ 拖动时禁止选中文字 */
}
body.float-mode .dl-header h2 { pointer-events: none; }
/* ② 可交互元素必须反声明，否则点不到 */
body.float-mode .dl-header button { -webkit-app-region: no-drag; }
```

> frameless 窗口默认保留 `resizable: true`，拖窗口边缘即可调整大小，**无需额外代码**。

---

## 五、验证方法（先自动化，再人工）

### 自动化：无头渲染断言

用项目自带 Electron 的 offscreen 模式 + `executeJavaScript` 断言计算样式，**无需启动完整应用**：

```js
const win = new BrowserWindow({ show: false, webPreferences: { offscreen: true } });
await win.loadFile("renderer/index.html", { query: { view: "float" } });
const res = await win.webContents.executeJavaScript(`(() => {
  const header = document.querySelector('.dl-header');
  return JSON.stringify({
    headerRegion: getComputedStyle(header).webkitAppRegion,   // 期望 "drag"
    addBtnRegion: getComputedStyle(document.getElementById('addDlBtn')).webkitAppRegion, // "no-drag"
    gripVisible: getComputedStyle(document.getElementById('dragGrip')).display !== 'none',
    floatControlsVisible: getComputedStyle(document.getElementById('floatControls')).display,
  });
})()`);
```

断言清单：

| 项 | 期望（float 模式） | 期望（dock 模式） |
|---|---|---|
| `header.webkitAppRegion` | `drag` | `none` |
| 头部按钮 `webkitAppRegion` | `no-drag` | — |
| 拖拽把手 | 可见 | 隐藏 |
| 悬浮控制按钮 | `flex` | 隐藏 |

### 人工验证（自动化覆盖不到的）

启动应用 → 悬浮窗口 → ① 拖头部移动 ② 拖边缘调大小 ③ 最小化 ④ 停靠回主窗。app-region 拖拽是 OS 级行为，无法无头模拟，必须人工确认。

---

## 六、踩坑记录（方法论的核心价值）

### 坑 #1：`[hidden] { display: none !important; }` 全局规则压死一切

**现象**：float 模式下手动设置 `body.float-mode #floatControls { display: flex }`（特异性更高），按钮区仍然不显示。

**根因**：`style.css` 第 154 行存在既有规则 `[hidden] { display: none !important; }`。`floatControls` 在 HTML 里带 `hidden` 属性，`!important` 的优先级压过了普通作者样式——**悬浮窗口的「停靠回主窗口/关闭」按钮其实从未显示过**（既有 bug）。

**修复**（双保险）：
```js
floatControls.hidden = false;   // 语义正确：JS 显式解除隐藏
```
```css
body.float-mode #floatControls { display: flex !important; }  // CSS 兜底
```

**方法论**：排查"样式没生效"问题时，先 `grep -n "!important" style.css` 全局搜一遍，再怀疑浏览器。

### 坑 #2：`app-region: drag` 会吞掉区域内所有鼠标事件

**现象**：拖拽区域内的按钮点了没反应。

**根因**：drag 区域的点击事件被 OS 用于拖窗，不会派发给页面。

**修复**：可交互元素逐个声明 `-webkit-app-region: no-drag`。**每条 drag 规则旁边必须跟上 no-drag 注释**，防止后人漏加。

### 坑 #3：最小复现实验可能骗你

**现象**：同样的 CSS 规则，最小测试页显示 `flex`，真实页面显示 `none`。

**根因**：最小页没有全局规则 `[hidden] { display:none !important }`，复现不了。

**方法论**：对照实验要**在真实页面上做**（动态注入规则 / 移除属性对比），不要只信最小复现；两步定位：`el.matches(selector)` 确认规则匹配 → 动态移除属性/注入规则确认因果。

### 坑 #4：Windows 下测试脚本别放 `/tmp`

**现象**：Electron 测试脚本放 git-bash 的 `/tmp` 路径，进程超时挂起。

**根因**：Windows 上 Electron 解析不了 `/tmp/xxx.mjs`。

**方法论**：测试脚本放项目内（如 `UI/test-anim/`），跑完删掉。

---

## 七、可复用模板：Electron「拖拽控制」通用三件套

任何"拖拽控制窗口/面板"的功能，都按这个模板落地：

```
① 主进程      —— 窗口操作 IPC（move / resize / minimize / setAlwaysOnTop）
② preload.js  —— 安全桥接，渲染进程绝不直接拿 BrowserWindow
③ 渲染进程    —— 拖拽区域声明（app-region: drag）+ 交互元素反声明（no-drag）
                  + 拖拽把手 UI + 禁用选中
```

配套流程：

```
需求 → 盘点现状（已有能力表）→ 进程边界分析 → 方案选型对比表
     → 四步实现 → 无头断言自动化验证 → 人工验证 OS 级行为 → 沉淀文档
```

---

## 八、扩展方向

- **记住窗口位置**：`win.getPosition()` + `localStorage` 持久化，重启恢复
- **拖到屏幕边缘自动停靠**：监听悬浮窗口 `move` 事件，与主窗口边缘碰撞时 `setBounds` 吸附
- **停靠面板头部拖拽换方向**：复用 `dock-side-btn` 的点击逻辑，改 pointer 拖拽触发
- **多显示器支持**：`screen.getAllDisplays()` 约束窗口边界
- **置顶小窗模式**：`setAlwaysOnTop(true)`，下载时悬浮在角落
